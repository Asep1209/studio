// Firebase is disabled in favor of local browser storage (IndexedDB).
export const isFirebaseConfigured = false;

// The rest of the exports are kept as undefined to avoid breaking imports in other files.
export const app = undefined;
export const db = undefined;
export const storage = undefined;
export const auth = undefined;
